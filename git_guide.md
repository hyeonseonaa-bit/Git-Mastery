# 🌿 Git Mastery

---

## 1. Git 환경설정 기본 개념

### 알아두면 좋은 것들

- **`.gitignore`**: `폴더명/**` 패턴으로 특정 폴더를 Git 추적에서 제외할 수 있다.
- **`cd ..`**: 터미널에서 상위 폴더로 이동하는 명령어.

---

## 2. Git 기본 사용 흐름 (Branch 전략)

> ⚠️ **실제 프로젝트 에서는 되도록 직접 main/master에 push하지 말 것! 항상 branch를 만들어서 작업한다.**
> **Sync나 Publish 버튼 사용 시 에러 원인 파악하기 힘들 수 있으니 되도록 코드 사용할 것 **

```
① git checkout -b (새로운 branch 이름)
   → 해당 branch를 만들면서 바로 이동
   
② git branch
   → 현재 내가 있는 branch 확인
   
③ git add (폴더명)/
   → 변경사항 스테이징
   
④ git commit -m "feat: ~ "
   → commit message 작성 (아래 규칙 참고)
   
⑤ git push origin (현재 branch)
   → 원격 저장소에 push
   
⑥ GitHub에서 새로고침
   → 새로운 branch에 파일이 올라온 것을 확인
   
⑦ main (또는 master)와 merge 한다
   
⑧ Delete Branch (merge 후 branch 삭제)

⑨ git checkout master
   → 로컬에서 master 브랜치로 이동
   
⑩ git pull origin master
   → 원격의 최신 상태를 로컬에 반영
   → GitHub 웹에서의 타임라인과 VS Code에서의 타임라인을 맞춘다
```

---

## 3. Commit Message 규칙

> ⭐ **수정할 때는 commit-message 규칙을 반드시 잘 따르자!**

타입은 해당 커밋의 성격을 나타내며, 아래 중 하나여야 한다.

| 타입 | 설명 |
|------|------|
| `feat` | 새로운 기능에 대한 커밋 |
| `fix` | 버그 수정에 대한 커밋 |
| `build` | 빌드 관련 파일 수정 / 모듈 설치 또는 삭제에 대한 커밋 |
| `chore` | 그 외 자잘한 수정에 대한 커밋 |
| `ci` | ci 관련 설정 수정에 대한 커밋 |
| `docs` | 문서 수정에 대한 커밋 |
| `style` | 코드 스타일 혹은 포맷 등에 관한 커밋 |
| `refactor` | 코드 리팩토링에 대한 커밋 |
| `test` | 테스트 코드 수정에 대한 커밋 |
| `perf` | 성능 개선에 대한 커밋 |

**작성 예시:**
```
git commit -m "feat: 로그인 기능 추가"
git commit -m "fix: 회원가입 버튼 클릭 오류 수정"
git commit -m "docs: README 업데이트"
```

---

## 4. 중요 원칙들

### ✅ 컴퓨터 파일 구조 = Git 파일 구조

로컬(컴퓨터)의 폴더/파일 구조와 Git(원격) 저장소의 구조가 **반드시 일치**해야 한다.

- 로컬에서 폴더 위치를 바꾸면 Git에서도 그 변경을 추적해서 반영해야 한다.
- 구조가 어긋나면 push/pull 시 충돌이 발생할 수 있다.

---

### ✅ Merge 조건

Merge를 하려면 두 가지 조건이 필요하다.

1. **기본 구조(Base)가 같아야 한다** — 같은 base commit에서 분기된 branch여야 한다.
2. **변경 사항이 있어야 한다** — 변경된 내용이 없으면 merge할 의미가 없고 conflict가 발생할 수 있다.

---

## 5. ⚠️ `.git` 중복 연결 문제 (중요 트러블슈팅)

### 문제 상황

프로젝트 폴더 안에 또 다른 `.git` 폴더가 있거나, 이미 다른 Git 저장소와 연결되어 있을 때 발생하는 문제.

예시:
```
my-project/        ← .git 있음 (상위 저장소)
  └── sub-folder/  ← .git 있음 (또 다른 저장소 ← 문제!)
```

이런 경우 push/pull이 꼬이거나, sub-folder가 제대로 추적되지 않는다.

---

### 진단 방법

```bash
# 숨김 파일(.git 등)까지 모두 확인
ls -al
```

`ls -al` 을 통해 `.git` 폴더가 존재하는지, 예상치 못한 위치에 있는지 확인한다.

---

### 해결 방법: `.git` 연결 끊기

```bash
# 해당 폴더의 .git 디렉토리 강제 삭제 (Git 연결 해제)
rm -rf .git
```

> ⚠️ **주의:** `rm -rf`는 되돌릴 수 없다. 반드시 올바른 경로에서 실행하고 있는지 확인 후 실행할 것!

---

### 자주 발생하는 이슈 & 해결책

| 이슈 | 원인 | 해결책 |
|------|------|--------|
| sub-folder가 회색으로 보이며 추적 안 됨 | 하위 폴더에 별도 `.git`이 존재 (submodule 문제) | 해당 폴더로 이동 후 `rm -rf .git` 실행 |
| `push`했는데 파일이 안 올라감 | 상위/하위 `.git` 충돌 | `ls -al`로 `.git` 위치 확인 후 불필요한 것 삭제 |
| `git init`을 잘못된 경로에서 실행 | 엉뚱한 폴더가 Git 저장소가 됨 | 해당 폴더에서 `rm -rf .git` 으로 초기화 취소 |
| pull 시 "not a git repository" 오류 | 현재 폴더에 `.git`이 없음 | 올바른 프로젝트 루트로 이동 후 명령 실행 |
| merge conflict 발생 | 같은 파일의 같은 부분이 두 branch에서 다르게 수정됨 | VS Code에서 충돌 표시 확인 후 수동으로 선택하여 해결 |

---

## 6. 추가 팁

### VS Code에서 Git 현재 위치 확인 플러그인 설치
현재 어느 branch에 있는지 하단 상태바에 표시되도록 Git 관련 플러그인을 설치해두면 편리하다.

### GitHub Gemini 연결로 편리하게 요약하기
GitHub에 Gemini를 연결하면 코드 변경사항이나 PR 내용을 AI가 자동으로 요약해준다.

### Repository 이해하기
- **Local Repository**: 내 컴퓨터에 있는 저장소 (`.git` 폴더가 있는 곳)
- **Remote Repository**: GitHub 등 원격 서버에 있는 저장소
- `push` = local → remote
- `pull` = remote → local

---

> 💡 **핵심 요약**: branch 만들어서 작업 → commit message 규칙 지키기 → push → PR → merge → pull로 동기화. 문제가 생기면 `ls -al`로 `.git` 확인부터!
